import React from 'react'; // eslint-disable-line no-unused-vars

// Top section removed: buttons moved into chat header within DeepTutorChat.
// Keeping a no-op component to avoid import breakages.
const DeepTutorTopSection = () => null;

export default DeepTutorTopSection;
